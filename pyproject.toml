[project]
name = "skysh-kulab"
version = "0.1.0"
description = "Upbit trade ingestion and Redis minute bucket writer"
requires-python = ">=3.12"
dependencies = [
  "websockets>=12.0"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

